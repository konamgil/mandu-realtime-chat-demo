{
  "name": "mandu-chat-demo",
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.3.9",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "routes:generate": "./node_modules/.bin/mandu routes generate",
    "dev": "mkdir -p .mandu/client && ./node_modules/.bin/tailwindcss -i app/globals.css -o .mandu/client/globals.css >/dev/null 2>&1 || true && PORT=3333 ./node_modules/.bin/mandu dev",
    "dev:safe": "./node_modules/.bin/mandu lock && mkdir -p .mandu/client && ./node_modules/.bin/tailwindcss -i app/globals.css -o .mandu/client/globals.css >/dev/null 2>&1 || true && PORT=3333 ./node_modules/.bin/mandu dev --watch",
    "build": "./node_modules/.bin/mandu build",
    "start": "./node_modules/.bin/mandu start",
    "check": "./node_modules/.bin/mandu check",
    "guard": "./node_modules/.bin/mandu guard",
    "lock": "./node_modules/.bin/mandu lock",
    "test": "bun test tests/*.test.ts",
    "test:unit": "bun test tests/*.test.ts",
    "test:e2e": "bunx playwright test",
    "record:setup": "bunx playwright install chromium",
    "record:demo": "bun run scripts/record-realtime-chat.ts",
    "test:ate:e2e": "bun run scripts/run-ate-e2e.ts",
    "record:scenarios": "bun run scripts/record-chat-scenario.ts 01-login-send && bun run scripts/record-chat-scenario.ts 02-offline-reconnect && bun run scripts/record-chat-scenario.ts 03-longtext-guard"
  },
  "dependencies": {
    "@mandujs/core": "0.19.0",
    "@mandujs/mcp": "^0.18.9",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@mandujs/ate": "^0.17.2",
    "@mandujs/cli": "^0.18.10",
    "@playwright/test": "^1.58.2",
    "@tailwindcss/cli": "^4.2.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "bun-types": "^1.3.9",
    "playwright": "^1.58.2",
    "tailwindcss": "^4.2.0",
    "typescript": "^5.9.3"
  },
  "overrides": {}
}
