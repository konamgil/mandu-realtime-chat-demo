{
  "name": "mandu-chat-demo",
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.2.0",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "dev": "bunx mandu dev",
    "dev:safe": "bunx mandu lock && bunx mandu dev --watch",
    "build": "bunx mandu build",
    "start": "bunx mandu start",
    "check": "bunx mandu check",
    "guard": "bunx mandu guard",
    "lock": "bunx mandu lock",
    "test": "bun test"
  },
  "dependencies": {
    "@mandujs/core": "^0.13.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@mandujs/cli": "^0.15.1",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "typescript": "^5.0.0"
  }
}
